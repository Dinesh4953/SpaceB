services:
  - type: cron
    name: meal-booking-cron
    env: python
    plan: free
    schedule: "0 8 * * *"  # Daily at 8 AM UTC (adjust as needed)
    buildCommand: pip install -r requirements.txt
    startCommand: python web.py
    envVars:
      - key: DISPLAY
        value: ":99"